import Section from "@/components/Section";
import SectionHeader from "@/components/SectionHeader";

export default function About() {
  return (
    <Section width="narrow">
      <SectionHeader
        as="h1"
        headline="About"
        subheadline="[Mission, background on Sebastian and Noah, the Securus collaboration workflow, and the inclusion-and-second-chances framing.]"
      />
    </Section>
  );
}
