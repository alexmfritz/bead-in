import Section from "@/components/Section";
import SectionHeader from "@/components/SectionHeader";

export default function Gallery() {
  return (
    <Section>
      <SectionHeader
        as="h1"
        headline="Gallery"
        subheadline="[All items including sold and reserved, shown in a lightbox-enabled image grid.]"
      />
    </Section>
  );
}
