export default function Shop() {
  return (
    <section className="mx-auto max-w-5xl px-4 py-12">
      <h1 className="mb-4 text-4xl font-semibold">Shop</h1>
      <p className="text-lg text-text-muted">
        [Shop — coming soon. Will list every item with{" "}
        <code>status === "available"</code> in a grid with filter controls
        (artist, materials, tags, price range).]
      </p>
    </section>
  );
}
